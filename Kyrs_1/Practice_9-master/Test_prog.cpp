/*#include "Template.h"
#include <iostream>

int main() {
	setlocale(LC_ALL, "Russian");

	int x = 1; int y = 2;
	std::cout << "Перед обменом (int):\n";
	outXY(x, y);
	swap(x, y);
	std::cout << "После обмена:\n";
	outXY(x, y);
	float x1 = 1.5; float y1 = 5.9;
	std::cout << "Перед обменом (float):\n";
	outXY(x1, y1);
	swap(x1, y1);
	std::cout << "После обмена:\n";
	outXY(x1, y1);
	My_class t1(1, 1.5), t2(2, 9.9);
	std::cout << "Перед обменом (class):\n";
	t1.Out();
	t2.Out();
	swap(t1, t2);
	std::cout << "После обмена:\n";
	t1.Out();
	t2.Out();
}*/